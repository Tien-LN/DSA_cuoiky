#ifndef MENU_HPP
#define MENU_HPP

#include <string>

// Hiển thị menu chính của chương trình
void displayMenu(bool dataLoaded, bool hasInvalidRows, bool dataStructureSelected, std::string dsName);

#endif